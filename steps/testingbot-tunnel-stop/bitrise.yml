format_version: "11"
default_step_lib_source: https://github.com/bitrise-io/bitrise-steplib.git

app:
  envs:
  # Used by `share-this-step`.
  - BITRISE_STEP_ID: testingbot-tunnel-stop
  - BITRISE_STEP_VERSION: 1.0.0
  - BITRISE_STEP_GIT_CLONE_URL: https://github.com/testingbot/bitrise-step-testingbot-tunnel-stop.git
  - MY_STEPLIB_REPO_FORK_GIT_URL: $MY_STEPLIB_REPO_FORK_GIT_URL

workflows:
  test:
    summary: Checks that the Step stops a process and stays quiet otherwise.
    description: |-
      Needs no credentials: it stands up a placeholder long-running process and
      checks the Step shuts it down, then checks the two no-op paths that must
      never fail a build.
    steps:
    - script:
        title: Start a placeholder tunnel process
        inputs:
        - content: |-
            #!/usr/bin/env bash
            set -eo pipefail
            sleep 300 &
            echo "started pid $!"
            envman add --key FAKE_TUNNEL_PID --value "$!"

    - path::./:
        title: Stop it
        inputs:
        - tunnel_pid: $FAKE_TUNNEL_PID
        - shutdown_timeout: "10"

    - script:
        title: Check the process is gone
        inputs:
        - content: |-
            #!/usr/bin/env bash
            set -eo pipefail
            if kill -0 "$FAKE_TUNNEL_PID" 2>/dev/null; then
              echo "error: pid $FAKE_TUNNEL_PID is still running" >&2
              exit 1
            fi

    - path::./:
        title: Stopping an already-dead tunnel must succeed
        inputs:
        - tunnel_pid: $FAKE_TUNNEL_PID

    - path::./:
        title: An empty pid must succeed
        inputs:
        - tunnel_pid: ""

  check:
    summary: Lints step.yml and the README against each other.
    steps:
    - git::https://github.com/bitrise-steplib/steps-check.git:
        inputs:
        # These are bash Steps: there is no Go code to lint, and no Go unit
        # tests to run. The offline suite lives in the development monorepo.
        - workflow: lint
        - skip_go_checks: "yes"

  generate_readme:
    summary: Regenerates README.md from step.yml plus docs/.
    steps:
    - git::https://github.com/bitrise-steplib/steps-readme-generator.git@main:
        inputs:
        - example_section: docs/examples.md
        - contrib_section: docs/contributing.md

  audit-this-step:
    steps:
    - script:
        inputs:
        - content: |-
            #!/usr/bin/env bash
            set -eo pipefail
            stepman audit --step-yml ./step.yml

  share-this-step:
    description: |-
      Shares this version of the Step to a fork of the Bitrise StepLib.

      Set MY_STEPLIB_REPO_FORK_GIT_URL and bump BITRISE_STEP_VERSION above, tag
      the release in git, then run this workflow and open a pull request.
    envs:
    - BITRISE_STEP_ID: $BITRISE_STEP_ID
    - BITRISE_STEP_VERSION: $BITRISE_STEP_VERSION
    - BITRISE_STEP_GIT_CLONE_URL: $BITRISE_STEP_GIT_CLONE_URL
    - MY_STEPLIB_REPO_FORK_GIT_URL: $MY_STEPLIB_REPO_FORK_GIT_URL
    before_run:
    - audit-this-step
    steps:
    - script:
        inputs:
        - content: |-
            #!/usr/bin/env bash
            set -eo pipefail
            for var in MY_STEPLIB_REPO_FORK_GIT_URL BITRISE_STEP_ID BITRISE_STEP_VERSION BITRISE_STEP_GIT_CLONE_URL; do
              if [ -z "${!var}" ]; then
                echo "error: \$$var is not set" >&2
                exit 1
              fi
            done
            bitrise share start -c "$MY_STEPLIB_REPO_FORK_GIT_URL"
            bitrise share create --stepid "$BITRISE_STEP_ID" \
              --tag "$BITRISE_STEP_VERSION" \
              --git "$BITRISE_STEP_GIT_CLONE_URL"
            bitrise share finish
